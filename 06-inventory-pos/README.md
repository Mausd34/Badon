# Inventory & POS System

Product catalog, stock levels, low-stock alerts, sales checkout and inventory updates.

Run: `pip install -r requirements.txt && streamlit run app.py`

Production upgrade: PostgreSQL, barcode scanning, invoices, role-based access, payment gateway and audit logs.