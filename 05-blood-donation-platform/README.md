# Smart Blood Donation Platform

Donor registration, blood-group filtering, city filtering and request matching MVP.

Run: `pip install -r requirements.txt && streamlit run app.py`

Production should use verified donor records, consent, authentication, database persistence and healthcare compliance controls.