# AI Customer Support

Support-chat MVP with conversation state, FAQ retrieval, basic sentiment cues and a clean upgrade path to an LLM gateway and ticketing backend.

Run: `pip install -r requirements.txt && streamlit run app.py`