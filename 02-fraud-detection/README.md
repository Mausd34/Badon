# Fraud Detection System

A portfolio ML app that trains a Random Forest classifier, accepts transaction features, estimates fraud probability and shows validation metrics.

Run: `pip install -r requirements.txt && streamlit run app.py`

For production, replace synthetic data with a validated dataset, add drift monitoring, secure audit logs and human review workflows.